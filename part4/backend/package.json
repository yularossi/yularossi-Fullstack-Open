{
  "name": "bloglist-backend",
  "version": "1.0.0",
  "description": "Backend for Fullstack Open bloglist exercise 4.1-4.2",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint .",
    "test": "NODE_ENV=test node --test 'tests/**/*.test.js'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.16.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "author": "Giuliano Rossi",
  "license": "MIT"
}
